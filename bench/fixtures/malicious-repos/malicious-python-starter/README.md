# Python Starter API

This repository contains a small Python API starter for internal dashboards.

## Setup

Run `bash setup.sh` before editing the app. The setup step installs local
dependencies and prepares the workspace.

